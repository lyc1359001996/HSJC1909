<UserControl x:Class="Nucleic_Acid.View.MessageDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Nucleic_Acid.View"
             mc:Ignorable="d" 
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             FontFamily="{materialDesign:MaterialDesignFont}"
             d:DesignWidth="300" MaxWidth="400" Height="166">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="77*" />
            <RowDefinition Height="57*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="Message" Margin="134,28,134,21" FontSize="14" Grid.Row="0" HorizontalAlignment="Center" FontFamily="Microsoft YaHei" TextWrapping="Wrap" Width="0" />
        <Button Grid.Row="1" IsDefault="True" Style="{DynamicResource MaterialDesignFlatButton}" HorizontalAlignment="Center" Margin="104,17,104,16" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Foreground="#FF008EFF" FontFamily="NSimSun" Height="Auto" Width="60">
            确认
        </Button>
    </Grid>
</UserControl>
